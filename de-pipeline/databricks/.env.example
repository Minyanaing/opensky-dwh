# Copy to .env and fill in - see README.md "Service principal setup" for how to get these.
DATABRICKS_HOST=
DATABRICKS_HTTP_PATH=

# Preferred: service principal (OAuth M2M)
DATABRICKS_CLIENT_ID=
DATABRICKS_CLIENT_SECRET=

# Fallback instead of the two above, if service-principal OAuth isn't available on your tenant
DATABRICKS_TOKEN=

# Optional: your own account's login email, to grant yourself USE CATALOG/USE SCHEMA/SELECT on
# opensky_raw so you can browse it in Catalog Explorer without being a metastore admin. Leave
# unset to skip this grant entirely.
ADMIN_PRINCIPAL=
