-- {{CATALOG}} substituted per environment by env_setup.py --catalog. Drops the tables created by
-- tables_master.sql - schema/catalog untouched.
DROP TABLE IF EXISTS {{CATALOG}}.master.date;
DROP TABLE IF EXISTS {{CATALOG}}.master.time;
